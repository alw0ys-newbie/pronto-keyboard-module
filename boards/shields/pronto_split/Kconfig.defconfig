if SHIELD_PRONTO_SPLIT_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "pronto_split"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_PRONTO_SPLIT_LEFT || SHIELD_PRONTO_SPLIT_RIGHT

config ZMK_SPLIT
    default y

endif